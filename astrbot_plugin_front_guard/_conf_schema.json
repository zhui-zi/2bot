{
  "classifier_enabled": {
    "description": "Classify every private or bot-directed text message with DeepSeek Flash before routing.",
    "type": "bool",
    "default": true
  },
  "harassment_bypass_group_ids": {
    "description": "Group IDs that bypass harassment detection while retaining command routing and prompt-injection defense.",
    "type": "list",
    "default": []
  },
  "flash_provider_id": {
    "description": "AstrBot provider ID used by the front classifier.",
    "type": "string",
    "default": "deepseek-flash"
  },
  "flash_fallback_provider_id": {
    "description": "AstrBot provider ID used when the primary Flash provider fails.",
    "type": "string",
    "default": "deepseek-flash-official"
  },
  "classifier_timeout_seconds": {
    "description": "Maximum time allowed for a Flash classification request.",
    "type": "float",
    "default": 8
  },
  "command_min_confidence": {
    "description": "Minimum Flash confidence required to route a natural command.",
    "type": "float",
    "default": 0.78
  },
  "security_min_confidence": {
    "description": "Minimum Flash confidence required to block ambiguous harassment or injection.",
    "type": "float",
    "default": 0.88
  },
  "security_reply_timeout_seconds": {
    "description": "Maximum time allowed for a Flash-generated security reply.",
    "type": "float",
    "default": 8
  },
  "security_reply_temperature": {
    "description": "Sampling temperature for varied Flash-generated security replies.",
    "type": "float",
    "default": 0.9
  },
  "security_reply_max_tokens": {
    "description": "Maximum output tokens for a Flash-generated security reply.",
    "type": "int",
    "default": 96
  },
  "cache_ttl_seconds": {
    "description": "Optional classification cache lifetime, scoped to sender, session, quote, provider, and feature catalog. Zero sends every message to Flash.",
    "type": "float",
    "default": 0
  },
  "cache_max_entries": {
    "description": "Maximum number of hashed classification cache entries.",
    "type": "int",
    "default": 256
  }
}
